{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build HTTP Server",
            "type": "shell",
            "command": "${workspaceFolder}/build.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Build task for the HTTP server project"
        }
    ]
}
